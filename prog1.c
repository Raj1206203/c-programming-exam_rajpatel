#include<stdio.h>

int main()
{
    char elemnet;

    printf("enter an elemnet:%d",elemnet);

    for(i=1;i<elemnet;i++)
    {
        switch (1)
        {
        case : if(elemnet =)
            break;
           
        
        default:("the elemnet is incorect");
            break;
        }
    }

    
    return 0;
}